"use client";

/**
 * ForceLightMode - DEPRECATED
 * This component is kept for import compatibility but does nothing.
 * The site now uses dark mode exclusively.
 */
export function ForceLightMode() {
  // No-op: Site is dark mode only
  return null;
}
